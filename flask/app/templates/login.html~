{% extends "layout.html" %}

{% block content %}
  <div class="jumbo">
  <h1>Sign In</h1>
  <form method="POST" action="/commits">
      {{ form.hidden_tag() }}
      {{ form.name.label }} {{ form.name(size=20) }}
      <input type="submit" value="Go">
  </form>
  </div>
{% endblock %}
